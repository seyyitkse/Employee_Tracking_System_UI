@model LoginEmployeeDto

<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Login</title>
    <link rel="stylesheet" href="~/Project_Files/css/styles.min.css" />
    <link rel="icon" href="~/Project_Files/logos/favicon.ico" />
</head>

<body>
    <div class="page-wrapper" id="main-wrapper">
        <div class="position-relative overflow-hidden radial-gradient min-vh-100 d-flex align-items-center justify-content-center">
            <div class="d-flex align-items-center justify-content-center w-100">
                <div class="row justify-content-center w-100">
                    <div class="col-md-8 col-lg-6 col-xxl-3">
                        <div class="card mb-0">
                            <div class="card-body">
                                <a href="#" class="text-nowrap logo-img text-center d-block py-3 w-100">
                                    <img src="~/Project_Files/logos/PersoneLock-banner.png" width="180" alt="40">
                                </a>
                                <p class="text-center">Personel Takip Sistemi</p>
                                <form id="loginForm" method="post">
                                    <div asp-validation-summary="All"></div>
                                    <div class="mb-3">
                                        <label for="exampleInputEmail1" class="form-label">Mail</label>
                                        <input asp-for="Email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
                                        <span asp-validation-for="Email"></span>
                                    </div>
                                    <div class="mb-4">
                                        <label for="exampleInputPassword1" class="form-label">Şifre</label>
                                        <input asp-for="Password" class="form-control" id="exampleInputPassword1" type="password">
                                        <span asp-validation-for="Password"></span>
                                    </div>
                                    <div class="d-flex align-items-center justify-content-between mb-4">
                                        <div class="form-check">
                                            <input class="form-check-input primary" type="checkbox" value="" id="flexCheckChecked" checked>
                                            <label class="form-check-label text-dark" for="flexCheckChecked">
                                                Beni Hatırla
                                            </label>
                                        </div>
                                        <a class="text-primary fw-bold" href="#">Forgot Password?</a>
                                    </div>
                                    <button type="submit" class="btn btn-primary w-100 py-8 fs-4 mb-4 rounded-2">Giriş Yap</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="~/Project-Files/js/jquery/dist/jquery.min.js"></script>
    <script src="~/Project-Files/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById("loginForm").addEventListener("submit", async function (event) {
            event.preventDefault();

            const formData = new FormData(this);
            const loginData = {
                email: formData.get("Email"),
                password: formData.get("Password")
            };

            try {
                const response = await fetch("http://localhost:27312/api/Auth/Login", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify(loginData)
                });

                if (!response.ok) {
                    throw new Error("Login failed");
                }

                const responseData = await response.json();
                const token = responseData.token;

                document.cookie = "jwt=" + token + ";path=/";

                window.location.href = "/EmployeeHome/Index/";
            } catch (error) {
                console.error("Login error:", error);
            }
        });

        function logout() {
            document.cookie = "jwt=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
            window.location.href = "/LoginEmployee/LoginEmployee";
        }
    </script>
</body>

</html>
